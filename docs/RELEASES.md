# UNTORN Releases

## v3.3 (2026-04-28)

Highlights:
- Major architecture iterations across the pipeline modules.
- New modules added, including the trained edge matcher and supporting utilities.
- Updated trained model artifacts included under `models/`.
- Frontend and backend web app changes to match the new pipeline.

Notes:
- Docker images are tagged `v3.3` for both backend and frontend.
- Compose now references `ghcr.io/alisherbagyt/untorn-<service>:v3.3` by default.

