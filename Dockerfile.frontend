FROM node:20-alpine

WORKDIR /app

# Install deps separately so this layer caches when only source changes
COPY frontend/package*.json ./
RUN npm ci --omit=dev

COPY frontend/ .
RUN npm run build

EXPOSE 3000
CMD ["npm", "start"]
